#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define EL endl
#include "../dataStructs.h"


int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  Init();

  fill_n(vec.begin(), 2, -1);
  fill_n(deq.begin(), 2, -1);
  fill_n(arr.begin(), 2, -1);
  fill_n(str.begin(), 2, -1);
  fill_n(begin(c_arr), 2, -1);
  fill_n(lst.begin(), 2, -1);
  //fill_n(s.begin(), 2, -1);
  //fill_n(ms.begin(), 2, -1);
  //fill_n(mp.begin(), 2, -1);
  //fill_n(mmp.begin(), 2, -1);

  coutRAIt1();
  cout << '\n';
  coutBiAIt1();
  cout << '\n';

}

